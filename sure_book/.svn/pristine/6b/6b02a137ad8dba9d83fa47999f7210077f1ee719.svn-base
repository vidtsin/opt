from odoo import api, fields, models, _
import logging
_logger = logging.getLogger(__name__)

class pos_config(models.Model):
    _inherit = 'pos.config'

    iface_room_no = fields.Boolean('Room No.', default=True, help='Allow custom Room no. on POS')

#class pos_config(models.Model):
#    _inherit = 'pos.config'
#
#    iface_room_no = fields.Boolean('Room No.', default=True, help='Allow custom Room no. on POS')


