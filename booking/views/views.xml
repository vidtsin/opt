<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="backpack.list">
      <field name="name">backpack list</field>
      <field name="model">backpack.backpack</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="backpack.action_window">
      <field name="name">backpack window</field>
      <field name="res_model">backpack.backpack</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="backpack.action_server">
      <field name="name">backpack server</field>
      <field name="model_id" ref="model_backpack_backpack"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="backpack" id="backpack.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="backpack.menu_1" parent="backpack.menu_root"/>
    <menuitem name="Menu 2" id="backpack.menu_2" parent="backpack.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="backpack.menu_1_list" parent="backpack.menu_1"
              action="backpack.action_window"/>
    <menuitem name="Server to list" id="backpack" parent="backpack.menu_2"
              action="backpack.action_server"/>
    -->
  </data>
</odoo>